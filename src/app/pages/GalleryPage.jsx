import {Box, Container} from "@mui/material";

export const GalleryPage = () => {
  return(
    <Box id="GalleryPage">
        <Container>
            This is gallery page.
        </Container>
    </Box>
  );
}