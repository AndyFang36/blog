import {Box, Typography} from "@mui/material";

export default function ReactIntroPage() {
  return(
    <Box id="IntroPage">
      <Typography variant="h1">React</Typography>
      <Typography variant="h6">A JavaScript library for building user interfaces</Typography>
    </Box>
  )
}
