import { Box } from "@mui/material"

export const HomePage = () => {
    return (
        <Box>
            This is home page.
            Test: <code>console.log("Hello!");</code>
        </Box>
    );
}