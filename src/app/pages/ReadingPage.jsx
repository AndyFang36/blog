import {Box, Container} from "@mui/material";

export const ReadingPage = () => {
  return(
    <Box id="ReadingPage">
        <Container>
            This is reading page.
        </Container>
    </Box>
  );
}