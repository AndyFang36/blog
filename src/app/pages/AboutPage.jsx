import {Box, Container} from "@mui/material";

export const AboutPage = () => {
  return(
    <Box id="AboutPage">
        <Container id="AboutContainer">
            This is about page.
        </Container>
    </Box>
  );
}